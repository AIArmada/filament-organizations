# AIArmada Filament Organizations

Filament v5 administration for `aiarmada/organizations`.

Register `FilamentOrganizationsPlugin::make()` on the panel. The resource
navigation reads `filament-organizations.navigation.group` and
`filament-organizations.navigation.sort` through `getNavigationGroup()` and
`getNavigationSort()`.
